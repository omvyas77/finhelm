name: eval-gate

# Three tiers, and the shape was decided by a measurement rather than by the plan.
#
# The intent was two: a free deterministic tier on every push, and a paid judged tier on
# pull requests. The free tier was to run a real retrieval eval, because a gate that only
# re-reads recorded numbers gates nothing about the code in the diff.
#
# It does run one. It just cannot run it on every push: on a 2-vCPU runner the 40-question
# fixture eval takes 49 s per question and projects to 33 minutes, which is not a tier
# anyone leaves switched on. So the eval moved to pull requests and the every-push tier is
# what it can honestly be — unit tests, including the tests that cover the gate logic
# itself, in about three minutes.
#
# What is lost is stated rather than glossed: a push straight to a branch with no PR gets
# no retrieval check. What is kept is that the check, when it runs, is real.

on:
  push:
  pull_request:

concurrency:
  # A second push to the same branch makes the first run's answer irrelevant.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.10"
  TORCH_VERSION: "2.13.0"
  EMBED_MODEL: BAAI/bge-base-en-v1.5
  RERANK_MODEL: BAAI/bge-reranker-base
  # The gate runs against the committed fixture corpus in data/ci, never the real one:
  # the real corpus is 192 MB of chunk parquet plus 961 MB of index, both gitignored, so a
  # runner has neither. See scripts/make_ci_fixture.py.
  FINHELM_DATA_DIR: ${{ github.workspace }}/data/ci
  FINHELM_DEVICE: cpu
  # Pinned, but to 2 rather than 1, and the distinction cost a timeout to learn.
  #
  # faiss-cpu and torch each ship an OpenMP runtime. On macOS both live in one process and
  # collide, and dense retrieval segfaults; OMP_NUM_THREADS=1 is the fix there. Linux
  # wheels do not carry that conflict (verified Day 1), so importing the macOS workaround
  # into CI bought nothing and cost half the machine: the cross-encoder ran on one of two
  # vCPUs at 49 s/question, and the 40-question gate projected to 33 minutes.
  #
  # 2 matches the runner. If this ever does segfault on Linux, drop it to 1 and accept the
  # runtime — a loud crash is the signal, and it is not a silent one.
  OMP_NUM_THREADS: "2"

jobs:
  fast:
    name: tests
    runs-on: ubuntu-latest
    # Measured at ~3 minutes, almost all of it pip resolving 213 pinned packages.
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      # torch from the CPU index, exactly as the Dockerfile does it and for exactly the
      # same reason: ubuntu-latest is amd64, where the PyPI wheel bundles the CUDA runtime
      # and drags in ~2.5 GB of nvidia-* that this job can never use. --no-deps is what
      # makes the CPU index usable at all — --index-url replaces PyPI rather than adding
      # to it, so torch's ordinary Python dependencies have nowhere to resolve from.
      # requirements.txt supplies them in the next step.
      - name: Install torch (CPU build)
        run: |
          python -m pip install --upgrade pip
          pip install --no-deps "torch==${TORCH_VERSION}" \
            --index-url https://download.pytorch.org/whl/cpu

      - name: Install dependencies
        run: pip install -r requirements.txt

      # Nothing else notices an image or a runner quietly reacquiring the GPU stack.
      - name: Assert no CUDA packages were pulled in
        run: |
          leaked="$(pip list --format=freeze | grep -E '^(nvidia-|triton==|pytorch-triton)' || true)"
          if [ -n "$leaked" ]; then
            echo "::error::CUDA packages installed on a CPU-only runner:"
            echo "$leaked"
            exit 1
          fi
          python -c "import torch; print('torch', torch.__version__)"

      # -rs prints the reason for every skip. Two tests skip themselves when the real
      # index is absent, which is always true on a runner — without -rs the run is green
      # and silent about the fact that it never touched FAISS.
      - name: Unit tests
        run: pytest tests/ -q -rs -m "not slow"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-output
          path: evals/results/
          retention-days: 7
          if-no-files-found: ignore

  eval:
    name: deterministic retrieval gate
    # Pull requests only. See the note at the top: 33 minutes is not an every-push tier.
    if: github.event_name == 'pull_request'
    needs: fast
    runs-on: ubuntu-latest
    # 45, against a 33-minute projection. The first attempt at this used 25 and was
    # cancelled at question 27 of 40, which is how the 49 s/question figure was obtained.
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install torch (CPU build)
        run: |
          python -m pip install --upgrade pip
          pip install --no-deps "torch==${TORCH_VERSION}" \
            --index-url https://download.pytorch.org/whl/cpu

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Cache model weights
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-${{ env.EMBED_MODEL }}-${{ env.RERANK_MODEL }}

      # Keyed on the fixture itself, so regenerating data/ci invalidates the index rather
      # than silently evaluating new questions against an old corpus.
      # No index build step, and that is deliberate. Embedding the fixture measured at
      # 2 chunks/s on CPU — 1,131 filings chunks took 581 s — so building it here would
      # cost ~16 minutes on a runner with fewer cores than the machine that measured it.
      # Caching only moves that cost to whenever the cache is evicted or the fixture is
      # regenerated. The index is 9.3 MB, so it is committed instead: data/ci is laid out
      # as a data directory (processed/ beside index/) and FINHELM_DATA_DIR points
      # straight at it. CI embeds 40 queries, not 1,900 chunks.
      #
      # A committed index can go stale against the config that reads it, silently, because
      # _resolve_strategy substitutes `fixed` for a missing index rather than failing.
      # --fail-on-fallback below is what turns that into a red run.
      - name: Check the committed fixture is present
        run: |
          python - <<'PY'
          import sys
          sys.path.insert(0, "src")
          from finhelm.paths import INDEX_DIR, PROCESSED
          chunks = sorted(p.name for p in PROCESSED.glob("*.parquet"))
          indexes = sorted(p.name for p in INDEX_DIR.iterdir()) if INDEX_DIR.exists() else []
          print("chunks: ", chunks)
          print("indexes:", indexes)
          assert chunks and indexes, "data/ci fixture missing; regenerate with scripts/make_ci_fixture.py"
          PY

      # --deterministic-only is retrieve-only *and* no LLM router: no API key, no network,
      # no cost. Gating on recall@16 because 16 is what the service actually hands the
      # generator; the build guide's recall_at_5 is not a key this run produces, and
      # --fail-under treats an unknown metric as a failure rather than a pass precisely so
      # that gate cannot sit green forever.
      #
      # The floor is calibrated against the fixture corpus and means nothing against the
      # real one — ~1,900 chunks is a far easier retrieval problem than 24,650, so this
      # number reads higher than the headline 0.7403. It is a tripwire for "the diff moved
      # retrieval", not a quality measure.
      # No --agentic: --deterministic-only forces it off, because agent.decompose calls
      # the model and swallows every exception, so a keyless run would not fail — it would
      # quietly stop splitting multi-hop questions and report a number that looks real.
      # This tier therefore does not cover decomposition; the judged tier does.
      - name: Deterministic eval gate
        run: |
          python evals/run_eval.py \
            --chunking semantic --retriever hybrid --rerank --contextual \
            --embed-model "$EMBED_MODEL" --top-k-context 16 \
            --golden evals/ci_subset.jsonl \
            --deterministic-only --fresh --tag ci \
            --fail-on-fallback --allow-fallback complaints \
            --fail-under recall_at_16=0.80
        # 0.80 against 0.8667, measured on this exact fixture with this exact command.
        # Two questions of headroom: the run is deterministic, so identical code gives an
        # identical number and the only movement is real — but a single question flipping
        # is worth ~1/30, and a floor that trips on one is a floor nobody will keep.
        #
        # This is NOT the headline number and must not be quoted as one. Retrieval against
        # 1,903 chunks is a far easier problem than against 24,650; the full corpus scores
        # 0.7403. It is a tripwire for "the diff moved retrieval", nothing more.

      # if: always() — a failing gate is exactly when the per-question detail is worth
      # having, and a run that only uploads its artifacts on success is useless twice.
      - name: Upload eval results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-eval-results
          path: evals/results/
          retention-days: 14

  judged:
    name: judged smoke + regression
    if: github.event_name == 'pull_request'
    needs: fast
    runs-on: ubuntu-latest
    # 60, and the number is a consequence of a bug fix rather than of slow hardware.
    #
    # judge.py paces the judge at 12 RPM to stay inside the Gemini free tier — one call
    # every five seconds, globally, since the quota is per project per model.
    # ContextualRelevancyMetric judges every retrieved context, so the floor is
    # questions x k calls: 12 x 8 = 96 (~8 min) under the bare Config() this suite used
    # to score, and 12 x 16 = 192 (~16 min) under the config the service actually serves.
    # Faithfulness claims push the measured total to ~32 minutes.
    #
    # So correcting the gate to score the shipped config doubled its cost, and the "~6
    # minute" figure in the build guide describes a configuration nobody runs. The first
    # run after the fix was cancelled by a 30-minute limit.
    #
    # The lever is the 12 RPM, not the 16 contexts. The floor is questions x k x 5s, and
    # k is the term that must not move: judging faithfulness against only the passages the
    # answer cited measures self-consistency, not groundedness. A system that fabricates a
    # claim and cites a plausible-looking passage would pass cleanly, because the only
    # evidence examined is the evidence the answer selected. This repo has that exact
    # failure on file — q055 fabricates Jamie Dimon's compensation and cites a real 8-K
    # cover page, and scores citation_validity 1.0 today. Cited-only faithfulness would
    # likely score it well too: the cited page does not contradict the claim because it
    # does not address it. That would retire the one gate able to catch the failure this
    # system exists to prevent.
    #
    # Attack the rate instead, in this order:
    #   1. A paid Gemini tier. At 60 RPM the same suite is ~6 minutes — the build guide's
    #      own number — and the arithmetic collapses with nothing given up.
    #   2. A different judge model for the gate. This is a PR check, not a published
    #      metric; it needs to be consistent and fast, not identical to the judge used for
    #      a final evaluation.
    #   3. Fewer questions at full k. 6 x 16 costs what 12 x 8 did and preserves what the
    #      metric means. It weakens coverage, but plainly and statably.
    # If none are available, sample contexts at random rather than by citation: random
    # sampling is a weaker version of the same measurement, cited-only is a different
    # measurement wearing its clothes.
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install torch (CPU build)
        run: |
          python -m pip install --upgrade pip
          pip install --no-deps "torch==${TORCH_VERSION}" \
            --index-url https://download.pytorch.org/whl/cpu

      - name: Install dependencies
        run: pip install -r requirements.txt

      # The generator and the judge must stay in different model families — same-family
      # judging produces self-preference bias, and Config.__post_init__ enforces it. Both
      # keys are needed or the suite cannot run at all.
      # The suite skips itself without keys, which is right for a forked PR and wrong for
      # a branch in this repo — a silent skip there is a gate that stopped gating. This
      # makes the same-repo case loud.
      - name: Require judge credentials for same-repo pull requests
        if: github.event.pull_request.head.repo.full_name == github.repository
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: |
          missing=""
          [ -z "$ANTHROPIC_API_KEY" ] && missing="$missing ANTHROPIC_API_KEY"
          [ -z "$GOOGLE_API_KEY" ] && missing="$missing GOOGLE_API_KEY"
          if [ -n "$missing" ]; then
            echo "::error::judged tier cannot run without:$missing"
            echo "set them with: gh secret set <NAME> --repo ${{ github.repository }}"
            exit 1
          fi

      # Load-bearing: the suite disables DeepEval's per-attempt timeout (see the module
      # docstring for why a fixed number there is a guess), so nothing else bounds a hung
      # attempt. This job's timeout above is the only backstop left.
      - name: DeepEval smoke suite
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: pytest tests/test_smoke_deepeval.py -q -rs -m slow

      # Compares the newest history entry against evals/baseline.json. Distinct from the
      # floor above: a floor catches "this is bad", this catches "this is worse than what
      # we agreed to ship", which is the failure a generous floor lets through.
      - name: Regression against the accepted baseline
        run: python evals/compare_to_baseline.py --max-regression 0.03

      - name: Upload eval results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: judged-eval-results
          path: evals/results/
          retention-days: 14
